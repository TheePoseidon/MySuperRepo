
#include <stdio.h>

int main() {
    int number;

    //Enter a number
    printf("Enter a number: ");
    scanf("%d", &number);

    //If number is greater than 1000
    if (number > 1000) {
        printf("The number is greater than 1000./n");
    } else {
        printf("The number is greater than 1000./n");
    }

    return 0;
}